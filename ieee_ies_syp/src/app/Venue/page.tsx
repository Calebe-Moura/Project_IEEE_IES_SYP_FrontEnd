import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Venue", 
  description: "",
};

function Venue (){
  return(
    <div>
      <h1>Venue</h1>
    </div>
  )
}

export default Venue;