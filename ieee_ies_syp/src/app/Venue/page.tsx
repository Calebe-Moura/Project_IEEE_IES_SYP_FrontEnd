import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Venue", 
  description: "",
};

function Venue (){
  return(
    <div>
     
    </div>
  )
}

export default Venue;